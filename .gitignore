# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Model weights, datasets, and runtime state (4 GB+) — fetched by scripts/setup
.cache/

# Generated audio samples from benchmarks (bench/jfk.wav is a kept fixture)
bench/piper_*.wav
bench/tts_sample.wav
bench/*.log

# Trace output
*.jsonl

# Static site build output (regenerate with scripts/build_site.py)
dist/

# Personal learning material — kept local, not part of the published project.
# The handbook and exercise set are study aids, not shipped documentation.
docs/handbook/
exercises/
